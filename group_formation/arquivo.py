class Arquivo(object):
    def __init__(self, var, idt, iteracoes):
        super(Arquivo, self).__init__()
        self.var = str(var)
        self.idt = str(idt)
        self.iteracoes = str(iteracoes)
